'use strict';

{
  const swiper = new Swiper('.swiper', {
    grabCursor: true,
    watchSlidesProgress: true,
    loop: true,
    loopAdditionalSlides: 1,
    centeredSlides: true,
    autoplay: true,
    effect: 'fade',
    speed: 3000,
  });
}